# Copie este ficheiro para .env e preencha os valores.
# Nunca commite o ficheiro .env.

#--------------------------------------------------------------------
# GOOGLE OAUTH (login / cadastro)
# Google Cloud Console → APIs & Services → Credentials
# Redirect URI: {baseURL}/auth/google/callback
#--------------------------------------------------------------------
google.clientId =
google.clientSecret =
# google.redirectUri = https://academy.mechanical.co.mz/auth/google/callback
